1. Create a wrapper around axios that automatically includes the user's authtoken with requests
2. Create a "getById" function on the back-end
3. Break up pages into smaller pieces
4. Create "onboarding flow" framework